name: scanpy # feel free to change the name of the environment, but try to keep environment name and YAML file name consistent
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - anndata=0.11
  - scanpy=1.11
  - leidenalg
  - louvain
  - dask
  - dask-ml
  - sparse
  - zarr<3
  - ipykernel
  - requests
  - pip
  - pip:
    - git+https://github.com/mumichae/scAtlasTb_utils.git
    - pandas<2